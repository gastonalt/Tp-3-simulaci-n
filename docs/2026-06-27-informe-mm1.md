# Informe MM1 - 2026-06-27

## Contexto

Se continuo el trabajo luego del commit del hito experimental `M/M/1`. El objetivo fue avanzar hasta la parte 4 antes de comenzar el modelo de inventario:

1. Revisar las figuras SVG generadas.
2. Dejar commiteado el hito experimental `M/M/1`.
3. Preparar tablas LaTeX a partir de los resultados consolidados.
4. Redactar las secciones teoricas, metodologicas y de resultados preliminares del modelo `M/M/1`.

## Revision de figuras

La revision visual directa de los SVG no pudo hacerse con render local porque no habia una herramienta disponible para convertirlos a imagen raster. Se hizo una revision estructural de los archivos:

- Todos los SVG declaran tamanio `960x560`.
- No se encontraron valores invalidos como `NaN`, `undefined`, `None`, `null` o `Infinity`.
- Los graficos contienen titulos, ejes, leyendas, polilineas y puntos esperados.

Figuras revisadas:

- `figures/mm1/mm1_infinite_average_number_in_system.svg`
- `figures/mm1/mm1_infinite_average_number_in_queue.svg`
- `figures/mm1/mm1_infinite_average_time_in_system.svg`
- `figures/mm1/mm1_infinite_average_time_in_queue.svg`
- `figures/mm1/mm1_infinite_server_utilization.svg`
- `figures/mm1/mm1_denial_probability_by_capacity.svg`

Pendiente: antes del informe final conviene convertir SVG a PDF o PNG y revisar visualmente el resultado final.

## Commit realizado

Se realizo el commit:

```text
f01b56f feat: ejecutar experimentos MM1 completos
```

Este commit dejo versionados los experimentos completos, resultados CSV, graficos SVG y documentacion de la trampa de estabilidad para `rho >= 1`.

## Tablas LaTeX

Se agrego el generador:

```text
src/mm1/export_latex_tables.py
```

Comando usado:

```bash
python -m src.mm1.export_latex_tables
```

Tablas generadas:

- `report/tables/mm1_infinite_steady_state.tex`
- `report/tables/mm1_infinite_unstable.tex`
- `report/tables/mm1_denial_probability.tex`

Las tablas se generan desde:

```text
results/mm1/experiments/mm1_summary.csv
```

La tabla de cola infinita estable compara simulacion, intervalo de confianza, teoria y error porcentual para `rho = 0.25`, `0.50` y `0.75`.

La tabla de cola infinita inestable muestra `rho = 1.00` y `1.25`, pero marca que no hay referencia estacionaria.

La tabla de denegacion compara simulacion y teoria para colas finitas `B = 0`, `2`, `5`, `10` y `50`.

## Secciones LaTeX creadas

Se creo una primera estructura compilable para el informe:

- `report/main.tex`
- `report/sections/01-introduccion.tex`
- `report/sections/02-marco-teorico-mm1.tex`
- `report/sections/03-metodologia-mm1.tex`
- `report/sections/04-resultados-mm1.tex`

Contenido principal:

- Supuestos del modelo `M/M/1`.
- Formulas teoricas de cola infinita estable.
- Aclaracion explicita de que `rho >= 1` no tiene regimen estacionario en cola infinita.
- Formulas de cola finita `M/M/1/K`.
- Definicion de capacidad de cola `B` y capacidad total `K = B + 1`.
- Diseno experimental usado en Python.
- Logica de eventos discretos.
- Metricas registradas.
- Intervalos de confianza.
- Inclusion de las tablas LaTeX generadas.

## Validaciones

Se ejecuto:

```bash
python -m compileall src
```

Resultado: sin errores de sintaxis.

Tambien se ejecuto:

```bash
git diff --check
```

Resultado: sin problemas de whitespace.

Se revisaron referencias e inputs LaTeX con `rg`. Las referencias principales a tablas tienen labels existentes.

No se pudo compilar el PDF localmente porque no se encontraron `pdflatex`, `latexmk` ni `tectonic` instalados. La compilacion final queda para Overleaf o para una instalacion local de LaTeX.

## Decision sobre figuras en el informe

Por ahora las figuras SVG no se insertan directamente en `main.tex`. La razon es evitar dependencia de soporte SVG externo en Overleaf. Para la version final se recomienda convertirlas a PDF o PNG e incluirlas con `graphicx`.

## Pendientes inmediatos

1. Revisar el texto LaTeX del bloque `M/M/1`.
2. Decidir si las tablas tienen suficiente detalle o si conviene resumirlas para presentacion.
3. Convertir figuras SVG a PDF o PNG antes del armado final.
4. Comenzar la parte 5: modelo de inventario.
